import { createAnnotations } from "./annotations.js";

/*
  КОММЕНТАРИИ К СПРАВОЧНИКУ ФОРМАТА («Библия JSON», страница /bible).

  Отдельный файл, а не общий поток с замечаниями к страницам сайта: это разговор
  о ФОРМАТЕ («поле называется не так», «этого случая в описании нет»), а не о
  содержании страницы. Разбор у них тоже разный — замечания к страницам ждут
  правки в контенте и метки «сделано» из журнала разбора, а замечание к
  справочнику закрывается правкой самого документа.

  Смешать их значило бы засыпать очередь работы по страницам вопросами про
  формат, а разработчику — искать свои вопросы среди двух сотен чужих.
*/
const store = createAnnotations("bible-comments.json", "bible-comments");

export const getAll = store.getAll;
export const upsert = store.upsert;
export const remove = store.remove;
