import type { PageCards } from "./types";

/*
  «АУДИТОРИЯ ПРОГРАММЫ» — /ngo/audience, модуль m6-1.

  Как заполнять, показано в team.ts. Коротко: адрес блока — это
  «страница::раздел::вид::отпечаток», его удобно взять из карты блоков (/blocks).
  Блоки одной карточки должны идти ПОДРЯД и совпасть целиком, иначе карточка
  молча не соберётся.
*/

const SEGMENT = "/source/m6-1::kak-segmentirovat-auditoriyu-i-vydelit-ee-yadro";
const WHERE = "/source/m6-1::gde-iskat-auditoriyu";

export const ngoAudienceCards: PageCards = {
  page: "/ngo/audience",
  cards: [
    /*
      РАЗРЫВ МЕЖДУ ОЖИДАНИЯМИ И РЫНКОМ — замечание Юли от 11 августа 2026:
      «важное». Абзац подводит черту под всем разбором готовности аудитории:
      участник хочет работать «на компьютере», а рынок предлагает другое, и
      программе с этим разрывом жить. Обычным абзацем он теряется между двумя
      списками причин.

      Адрес НОВЫЙ: начало фразы переписано вторым заходом (см. rewrite в
      clientEdits/ngoAudience.ts) — «В программе важно учитывать…» под названием
      «Важно» повторяло само название. Прежний адрес — …paragraph::9uujbh.
    */
    { blocks: [`${SEGMENT}::paragraph::1ev4e7w`], title: "Важно" },

    /*
      ТАБЛИЦА «ГДЕ ИСКАТЬ АУДИТОРИЮ» — замечание Юли от 11 августа 2026:
      «давайте оформим карточками». В таблице две колонки и три строки: слева
      задача («если нужна молодая аудитория»), справа каналы. Читать её как
      таблицу незачем — строки друг с другом не сравнивают, каждая отвечает на
      свой случай.

      Делим на три карточки: строка = карточка, задача — названием, каналы —
      текстом (split в types.ts). Число обязано сойтись с источником: строк
      станет больше или меньше — раскладка оставит таблицу и скажет об этом
      заметкой на странице.
    */
    { blocks: [`${WHERE}::table::imc3al`], split: 3 },
  ],
};
