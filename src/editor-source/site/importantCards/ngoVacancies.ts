import type { PageCards } from "./types";

/*
  «Поиск вакансий» — /ngo/candidates/vacancies, модуль m6-2.

  КАРТОЧКИ «ВАЖНО» ПО ЗАМЕЧАНИЯМ КЛИЕНТА. На каждом из этих абзацев стоит
  замечание «важное»: мысль подводит черту под разбором и обычным текстом
  теряется между соседними абзацами. Жёлтая карточка с названием «Важно» её
  выделяет.

  Адрес блока — «страница::раздел::вид::отпечаток», он же виден в карте блоков
  (/blocks). Блоки каждой карточки взяты по одному: клиент отметил ровно один
  абзац, и захватывать соседние нельзя.
*/
export const ngoVacanciesCards: PageCards = {
  page: "/ngo/candidates/vacancies",
  cards: [
    /*
      «Помните, что один и тот же кандидат может успешно справляться…»

      Адрес НОВЫЙ: начало фразы переписано по замечанию (см. rewrite в
      clientEdits/ngoVacancies.ts), а отпечаток блока считается по его чистому
      тексту. Прежний адрес — …paragraph::t7ycdg.
    */
    { blocks: ["/source/m6-2::shag-1-opredelenie-prioritetov::paragraph::1oijgls"], title: "Важно" },
    /* «Заранее обсудите с кандидатом, какую информацию о себе…» — адрес новый,
       прежний …paragraph::1d4yiwj. */
    { blocks: ["/source/m6-2::shag-3-pomosch-kandidatu-v-ponimanii-opisaniya-v::paragraph::1ni6xzd"], title: "Важно" },
    /*
      СПИСОК ПЛОЩАДОК СО ЗНАЧКОМ ССЫЛКИ — замечание клиента msq4cbboc83f («без
      буллетов») с решением дизайнера «Иконки ссылки». В списке две строки, и
      обе — ссылки на сайты вакансий: HeadHunter и «Авито Работа». Точка перед
      ними ничего не говорит, а значок ссылки виден раньше, чем прочитано
      название площадки.

      Так же собраны соседние списки телеграм-каналов на этой же странице и
      перечень каналов в конце «О проекте».
    */
    {
      blocks: ["/source/m6-2::shag-2-poisk-vakansiy-na-podhodyaschih-ploschadk::list::eoo0u3"],
      target: "List",
      listIcon: "ссылка",
    },
    /*
      ТАБЛИЦА СТАЛА КАРТОЧКАМИ — замечание клиента «карточки». Строки друг с
      другом не сравнивают: каждая отвечает на свой случай, и читать их подряд
      как таблицу незачем. Строка становится карточкой: левая ячейка — название,
      правая — текст.

      Число обязано сойтись с источником. Станет строк больше или меньше —
      раскладка оставит таблицу и скажет об этом заметкой на странице.
    */
    { blocks: ["/source/m6-2::shag-1-opredelenie-prioritetov::table::nsev15"], split: 5 },
  ],
};
