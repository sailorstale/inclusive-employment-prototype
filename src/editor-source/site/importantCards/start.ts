import type { PageCards } from "./types";

/*
  «ИНКЛЮЗИВНОЕ ТРУДОУСТРОЙСТВО» — /general/start.

  Одна карточка по замечанию Мити от 11 августа 2026 («Собрать в карточку
  Важно»). Он выделил подпись «Прежде чем начинать, стоит ответить на несколько
  вопросов:» вместе с тремя вопросами под ней. Набранные так же, как всё вокруг,
  они читались очередным перечислением, хотя это развилка: с ответов на них
  начинается вся работа.

  ЭТОЙ ЖЕ КАРТОЧКОЙ ЗАКРЫТО СТАРОЕ ЗАМЕЧАНИЕ ЮЛИ (msg85d3x153d, 7 августа
  2026: «давай обыграем эти три вопроса, они важные, как-то выделим их»). В
  примечании к нему стояло решение «выдели жирным», но через день дизайнер
  выделил те же три вопроса карточкой. Жирный внутри жёлтой карточки был бы
  выделением поверх выделения, поэтому берём одно — карточку.

  В источнике три вопроса лежат ТРЕМЯ отдельными списками по одному пункту —
  отсюда три адреса подряд, а не один. Внутри карточки они склеиваются в один
  список (mergeSiblings), как это уже видно у карточки «Итоговые рекомендации»
  на этой же странице.
*/
const M = "/source/m1";
const S = `${M}::zachem-vnedryat-inklyuzivnoe-trudoustroystvo`;

export const startCards: PageCards = {
  page: "/general/start",
  cards: [
    {
      blocks: [
        `${S}::paragraph::1gpeaqk`,
        `${S}::list::13b5l2v`,
        `${S}::list::ld1ldk`,
        `${S}::list::1zezcj`,
      ],
      title: "Важно",
    },
  ],
};
