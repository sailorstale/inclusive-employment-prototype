import type { PageCards } from "./types";

/*
  «ШАГ 2. АУДИТ РАБОЧЕЙ СРЕДЫ» — /companies/hire/step-2.

  Заготовка: замечания клиента по этой странице разбираются отдельной сессией, и
  все её карточки «Важно» и «Пример» перечисляются здесь. Пока карточек нет —
  механика (index.ts) на пустой список ничего не делает.

  Как заполнять, показано в team.ts. Коротко: адрес блока — это
  «страница::раздел::вид::отпечаток», его удобно взять из карты блоков (/blocks).
  Блоки одной карточки должны идти ПОДРЯД и совпасть целиком, иначе карточка
  молча не соберётся.
*/
export const step2Cards: PageCards = {
  page: "/companies/hire/step-2",
  cards: [],
};
