import type { PageCards } from "./types";

/*
  «ШАГ 2. АУДИТ РАБОЧЕЙ СРЕДЫ» — /companies/step-2, модуль m5-1.

  Три замечания Юли от 10 августа 2026, каждое просит выделить абзац как важное:
  «оформим как важно» у нюанса про разный опыт экспертов и по одному слову
  «важное» у главной цели аудита и у напоминания про идеальную среду. У двух из
  трёх в пометке дизайнера стоит «Карточка Важно».

  Слово «Важно» — не украшение, а рабочее название: раскладка сама красит такую
  карточку жёлтым и ставит ей картинку «Важная информация» (bgFor и cardArt в
  contentTree).

  АДРЕСА ЗДЕСЬ СЧИТАНЫ ПО ПРАВЛЕНОМУ ТЕКСТУ, а не по источнику. Адрес блока — это
  отпечаток его текста, а карточки собираются последними, уже после правок по
  замечаниям (см. useModuleDoc). У двух из трёх абзацев текст как раз правится —
  из начала уходит ярлык, который стал названием карточки, — поэтому взять адрес
  из источника значило бы промахнуться и получить молча несобранную карточку.

  Отпечатки сверены с настоящим кодом: та же арифметика даёт совпадение с
  адресами, которые уже лежат в разметке дизайнера и в карточках на «Этике и
  коммуникации».
*/

const M5 = "/source/m5-1";
/* Первые две карточки лежат в главе шага, третья — в разделе типичных ошибок. */
const STEP = `${M5}::shag-2-vnutrenniy-audit-rabochey-sredy-processov`;
const MISTAKES = `${M5}::tipichnye-oshibki-rabotodateley-pri-provedenii-a`;

export const step2Cards: PageCards = {
  page: "/companies/step-2",
  cards: [
    /*
      Нюанс про разный опыт экспертов. Отпечаток r0elio — от текста уже без слов
      «Важный нюанс:» и без курсива (см. clientEdits/step2.ts).
    */
    { blocks: [`${STEP}::paragraph::r0elio`], title: "Важно" },

    /*
      Главная цель аудита. Юля выделила ДВА абзаца подряд («3.39+3.40»), и
      сначала карточка забирала оба. 11 августа 2026 Митя попросил вынести
      второй — «На первый взгляд аудит может показаться сложной задачей…»: он
      говорит не про цель аудита, а переводит к разделу про типичные ошибки.
      Внутри остаётся первый абзац, второй идёт обычным текстом сразу под
      карточкой.

      Текст абзаца не менялся, поэтому отпечаток тот же, что в источнике: в нём
      появилась только ссылка на шаблон технического задания, а разметка в
      отпечаток не входит.
    */
    { blocks: [`${STEP}::paragraph::1but2wi`], title: "Важно" },

    /*
      Напоминание про идеальную среду. Отпечаток 16lvmtd — от текста уже без слов
      «Важно помнить:».
    */
    { blocks: [`${MISTAKES}::paragraph::16lvmtd`], title: "Важно" },
  ],
};
