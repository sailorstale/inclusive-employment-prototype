import type { PageCards } from "./types";

/*
  «КАК УСТРОЕН НАЁМ» — /general/how.

  Одиннадцать карточек по замечанию Мити от 11 августа 2026: «Надо собрать это
  все в карточки. И данные упаковать человеческим языком». В источнике это
  таблица из четырёх колонок — город, имя, должность, форма инвалидности, — и
  читалась она реестром, а не рассказом о людях.

  АДРЕС ЗДЕСЬ ОТ НОВОЙ ТАБЛИЦЫ, И ЭТО НЕ ОПЕЧАТКА. Карточки собираются ПОСЛЕ
  правок текста, а таблицу мы пересобрали в две колонки (retable в
  clientEdits/how.ts). Отпечаток считается по ячейкам, поэтому у старой таблицы
  адрес заканчивается на a5late, а у новой — на 1ida6i0. Поменяете хоть букву в
  ячейке — пересчитайте адрес, иначе карточки молча не соберутся и на странице
  останется таблица.

  Деление на одиннадцать: у таблицы две колонки и одиннадцать строк, каждая
  строка становится карточкой — первая ячейка названием, вторая текстом.
*/
const M = "/source/m4";
const SEC = `${M}::chto-delat-esli-sotrudnichestvo-nko-i-rabotodate`;

export const howCards: PageCards = {
  page: "/general/how",
  cards: [{ blocks: [`${SEC}::table::1ida6i0`], split: 11 }],
};
