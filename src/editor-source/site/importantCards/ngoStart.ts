import type { PageCards } from "./types";

/*
  «ЗАПУСК ПРОГРАММЫ» — /ngo/start, модуль m6-1.

  Все карточки «Важно» и «Пример» этой страницы перечисляются здесь. Адрес блока —
  «страница::раздел::вид::отпечаток», его удобно взять из карты блоков (/blocks).
  Блоки одной карточки должны идти ПОДРЯД и совпасть целиком, иначе карточка
  молча не соберётся.
*/

const SECTION = "/source/m6-1::programma-trudoustroystva-v-nko";

/*
  ВОПРОСЫ ПЕРЕД ЗАПУСКОМ ПРОГРАММЫ — карточка «Важно».

  Два замечания на один и тот же кусок. Юля 10 августа 2026: «вот это например
  важный элемент текста — ему нужна визуальная поддержка». Митя 11 августа 2026,
  ответом на него: «Оформить в карточку Важно» — и показал уже все три блока
  сразу.

  Блоков три и они идут подряд: подводка «Перед тем как запускать программу
  инклюзивного трудоустройства, НКО важно честно ответить себе на несколько
  вопросов», сам список из четырёх вопросов и вывод «Если на часть вопросов пока
  нет ответа — это нормально». Забирать в карточку надо все три: список без
  подводки повисает, а вывод без списка теряет, о каких вопросах речь.

  Название «Важно» само даёт жёлтый фон и иллюстрацию «Важная информация»,
  отдельно их задавать не надо (см. index.ts).
*/
export const ngoStartCards: PageCards = {
  page: "/ngo/start",
  cards: [
    {
      blocks: [
        `${SECTION}::paragraph::wn94pi`,
        `${SECTION}::list::an10q3`,
        `${SECTION}::paragraph::1nqhrfg`,
      ],
      title: "Важно",
    },
  ],
};
