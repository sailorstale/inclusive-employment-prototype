import type { PageEdits } from "./types";

/*
  «Финансировать программу» — /ngo/funding, модуль m6-4.

  ПРАВОК ПО ЗАМЕЧАНИЯМ У СТРАНИЦЫ ПОКА НЕТ. Раньше здесь лежал список из четырёх
  заголовков («Простота языка» и соседние), которые снимались совсем, — так
  закрывали просьбу убрать их из меню справа. Дизайнер имел в виду другое:
  пометка «H4» значит, что строка остаётся заголовком, только мельче. Уровень
  теперь задаёт карта страницы (level в pageMap.ts), а файл остаётся заготовкой
  под будущие правки текста.
*/
export const ngoFundingEdits: PageEdits = {
  page: "/ngo/funding",
};
