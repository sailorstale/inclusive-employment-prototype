import type { PageEdits } from "./types";

/*
  «ШАГ 3. СОЗДАНИЕ ИНКЛЮЗИВНОЙ СРЕДЫ» — /companies/hire/step-3.

  Заготовка: замечания клиента по этой странице разбираются отдельной сессией,
  и все её правки текста, ячеек, вставки и смены вида блока пишутся сюда. Пока
  записей нет — механика (index.ts) на пустой набор ничего не делает.

  Что сюда класть, разобрано в types.ts. Готовые примеры на каждый случай — в
  соседних файлах: how.ts (переписанный текст, снятая разметка, вставки),
  contract.ts (ячейки таблицы), team.ts (жирный пункт списка, абзац стал
  заголовком).
*/
export const step3Edits: PageEdits = {
  page: "/companies/hire/step-3",
};
