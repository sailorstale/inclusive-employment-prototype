import type { PageEdits } from "./types";

/*
  «Развивать и масштабировать» — /ngo/scale, модуль m6-4.

  ЗАГОЛОВОК, СНЯТЫЙ СОВСЕМ. «Оценка готовности команды и ресурсов» перестаёт
  быть заголовком и остаётся на странице обычным текстом.

  «Чек-лист готовности команды» раньше снимали так же, и это было ошибкой.
  Дизайнер пометил его «H4» — значит, он остаётся заголовком, только мельче.
  Уровень теперь задаёт карта страницы (level в pageMap.ts).
*/
export const ngoScaleEdits: PageEdits = {
  page: "/ngo/scale",

  untype: ["Оценка готовности команды и ресурсов"],
};
