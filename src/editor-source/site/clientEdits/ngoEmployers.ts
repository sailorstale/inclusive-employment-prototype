import type { PageEdits } from "./types";

/*
  «Выходить на работодателей» — /ngo/employers, модуль m6-3.

  ЗАГОЛОВКИ, УБРАННЫЕ ИЗ ОГЛАВЛЕНИЯ СПРАВА — замечания клиента «убрать из меню
  справа». Строка остаётся на странице обычным текстом, а из оглавления уходит:
  туда идут только заголовки, у которых есть якорь (см. untype в types.ts).

  Все перечисленные заголовки в источнике встречаются по одному разу и стоят
  уровнем ниже второго, поэтому границы разделов страницы от их снятия не
  сдвигаются.
*/
export const ngoEmployersEdits: PageEdits = {
  page: "/ngo/employers",


  /*
    «ГДЕ ИСКАТЬ:» ЖИРНЫМ — замечание клиента. Строка вводит список площадок, и
    обычным текстом она сливается с абзацем выше. Меняется только разметка: сам
    текст прежний, поэтому адрес блока не сдвигается.
  */
  rewrite: {
    "Где искать:": { md: "**Где искать:**", text: "Где искать:" },
  },

  /*
    ТОЧКИ С ЗАПЯТОЙ В КОНЦЕ ПУНКТОВ — замечание клиента «убрать точки с запятой».
    В гугл-доке список был одним предложением через точку с запятой, а на
    странице каждый пункт стоит отдельной строкой, и знак в конце читается как
    опечатка. Текст пункта при этом не меняется — значит, не меняется и адрес
    списка, который считается по текстам пунктов.
  */
  listItemMd: {
    "региональные сайты вакансий;": "региональные сайты вакансий",
    "карьерные страницы компаний;": "карьерные страницы компаний",
  },

  untype: [
    "Проявляйте инициативу при поиске контактов",
  ],
};
