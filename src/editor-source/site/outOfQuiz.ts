/*
  ФРАЗА, ВЫНУТАЯ ИЗ КВИЗА, — блок остаётся на странице, но перестаёт быть частью
  разбора и читается отдельной строкой прямо под самим квизом.

  Пятый сосед в ряду: dropScaffold (блок вообще не доезжает до страницы),
  cutFromCard (доезжает, но вне карточки), importantCards (доезжает карточкой),
  clientEdits (доезжает изменённым).

  ЗАЧЕМ. Замечания Юли от 11 августа 2026 («выделить как-то, так как это не
  стандартный квиз и эта фраза помогает понять что выбирать далее») и дизайнера
  от 16 августа 2026 («её надо вытащить из квиза и поставить под ним») — про одну
  и ту же строку: «Ответьте только на тот вопрос, который соответствует вашему
  ответу в первом пункте. Остальные вопросы можно пропустить».

  Она объясняет, КАК проходить самопроверку: из следующих четырёх вопросов надо
  отвечать на один, тот, что совпал с ответом в первом. Сейчас она лежит внутри
  разбора первого квиза, а разбор открывается только после ответа — то есть
  инструкция появляется тогда, когда следовать ей уже поздно.

  ПОЧЕМУ НЕ cutFromCard. Тот снимает блоки ТОЛЬКО С КОНЦА разметки и работает до
  раскладки: цепочка блоков обязана остаться непрерывной, иначе она не совпадёт
  с источником и разметка слетит со всего куска. Здесь блок стоит в середине —
  седьмым из двадцати семи.

  ПОЧЕМУ НЕ «ОТЦЕПИТЬ БЛОК ОТ РАЗМЕТКИ» после раскладки. Проверено сборкой
  16 августа 2026: группа рвётся надвое, во второй половине нет заголовка
  «Обратная связь», ветка сборки её не узнаёт — и четыре вопроса вместе со всем
  общим разбором молча исчезают. Квизов на странице становится три вместо семи,
  и ни одной заметки об этом не появляется.

  ПОЭТОМУ блок вынимает САМА сборка квиза (contentTree, ветка с «Обратной
  связью»): она эти блоки и так читает по порядку, и ей достаточно положить один
  из них рядом с квизом, а не в разбор. Цепочка при этом не трогается вовсе.

  Адресуем блок его собственным адресом, а не местом в разметке: так правка
  переживёт перевешивание разметки дизайнером. Поправят текст в источнике —
  адрес станет другим, и фраза молча вернётся в разбор; пересчитать адрес можно
  на странице карты блоков (/blocks).
*/
const OUT_OF_QUIZ = new Set<string>([
  // «Ответьте только на тот вопрос…» — самопроверка про масштаб аудитории на
  // странице «Аудитория программы».
  "/source/m6-1::s-kem-nachinat-rabotat::paragraph::5vy6mv",
]);

/** Вынимать ли этот блок из разбора квиза отдельной строкой под ним. */
export const liftsOutOfQuiz = (id: string): boolean => OUT_OF_QUIZ.has(id);
