node_modules/
dist/
*.local
.vite/

# build info
tsconfig.tsbuildinfo

# секреты
.env

# рантайм-данные сервера (правки/комментарии/решения — живут на томе Railway).
# Только корневая папка — не трогаем src/data (исходники навигации/глоссария).
/data/

.DS_Store
